#pragma once

#define UP (upKey == 1)
#define DOWN (downKey == 1)
#define LEFT (leftKey == 1)
#define RIGHT (rightKey == 1)
#define ACTION (actionKey == 1)

#define CPX currentPos.X
#define CPY currentPos.Y
#define MVX moveVector.X
#define MVY moveVector.Y